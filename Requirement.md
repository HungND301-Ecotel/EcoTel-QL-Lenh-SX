# Requirement — DIEU-PHOI-MM

Dự án này được **onboard từ source code có sẵn** (2026-08-24, GitHub `anhnt205/EcoTel-QL-Lenh-SX`), không tạo mới từ template — nên không có tờ trình/spec yêu cầu gốc để chép vào đây.

Yêu cầu/nghiệp vụ thật của hệ thống được **suy ra từ chính code đang chạy** (model, route, giao diện) và ghi lại đầy đủ tại [`docs/REQUIREMENTS_AS_IS.md`](docs/REQUIREMENTS_AS_IS.md) — đó là tài liệu tham chiếu chính cho dự án này, không phải file này.

File này chỉ tồn tại để thoả quy ước bắt buộc của factory (mỗi project phải có `Requirement.md`, xem `CLAUDE.md` ở gốc repo).
